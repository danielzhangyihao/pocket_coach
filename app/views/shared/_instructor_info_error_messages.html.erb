<% if @instructor_info.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@instructor_info.errors.count, "error") %>.
    </div>
    <ul>
    <% @instructor_info.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>